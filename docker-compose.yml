version: "3.7"

services:
  postgres:
    build: './database'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresadmin1234
      POSTGRES_DB: mygocqrsdb
    restart: always